---
name: bmod-cis
description: Required bmod metadata. Never invoke this skill.
---
This folder is the BMad Creative Intelligence Suite module's record, not something to run. Invoke the `bmad` skill with `setup cis`; it sets the module up if it never was, and otherwise reports its state. If there is no `bmad` skill, say so and offer `npx skills add bmad-code-org/BMAD-METHOD --skill bmad`.
